# 🚀 Connect Suite (社内チャット ✕ 電話連絡DX CallSync)

## 📌 プロジェクト概要
**Connect Suite** は、中小企業や現場・オフィス向けに、**「社内チャット（Google Chatリプレイス）」** と **「電話連絡DX（CallSync / 紙の電話メモ全廃）」** を1つの共通アカウント基盤（SSO）でシームレスに行き来・相互連携できるB2B SaaSスイートアプリです。

- **本番Webアプリ・API**: [https://connectsuite.easystance.app](https://connectsuite.easystance.app) ← **これが正式URL（2026-08-21〜）。ブックマークはこちらに変更してください**
  - 実体はCloudflare Workers（`callsync-backend`、フロント/バックエンド統合）。`https://callsync-backend.nonba30.workers.dev`でも同じものにアクセスできるが、URLに個人アカウント名が出てしまうため、`connectsuite.easystance.app`を正式な案内先とする（詳細は「固定サブドメイン追加」セクション参照）
- **クラウドDB (Cloudflare D1)**: `callsync-db`
- **リポジトリ**: `telephone-message-app` (OneDrive上: `C:\Users\nonba\OneDrive\telephone-message-app`)
- ⚠️ `https://callsync-app.pages.dev`（旧Cloudflare Pages配信）は**廃止・メンテ対象外**。中身は同一だが今後更新されない。詳細は「Pages廃止」セクション参照

---

## 📍 nonbaPCで続きをやる人へ：まずここだけ読めばOK（2026-08-21 PC03セッション終了時点）

**本番URLは`connectsuite.easystance.app`に変わった**（`callsync-app.pages.dev`はもう見なくていい、`workers.dev`のURLも動くが個人名が出るので案内には使わない）。デプロイは`npx wrangler pages deploy`ではなく**リポジトリルートで`npx wrangler deploy`**だけでOK（フロント・バックエンドが1つのWorkerに統合済み）。フロントは事前に`cd frontend && npm run build`してから。

**このセッションで確認済み・動いているもの**:
- Androidの戻る操作（スワイプ含む）→ アプリ終了せず正しく画面遷移。オーナー本人が実機で確認済み
- 電話メモの専用通知欄（チャットサイドバー最上部）→ 自分宛て・自分のグループ宛ての未対応メモだけ表示、その場で詳細確認・対応済みにできる、対応済みは表示から消える、件数が多い時は「電話メモアプリで全て見る」で本体アプリへ
- チャットの新着メッセージが開いたままでも数秒で反映される（前は開き直さないと見えなかったバグを修正済み）
- 1対1DMでは@メンションUIを非表示
- スマホ幅ではEnterキーが改行になり、送信は送信ボタンのみ（PCは今まで通りEnterで送信）

**要確認・引き継ぎ事項**:
- 電話メモ専用通知欄の最新版（対応済みを非表示にする変更＋「全て見る」リンク追加、`4c635c0`）は**まだオーナー本人による実機確認が済んでいない**。次に触った時に見た目を確認してほしい
- サブドメインログイン機能（会社ごとのURL）のコードは残っているが、実際のルーティングは意図的に外したまま（`easystance.app`を共有ドメインとして使うと他アプリを巻き込む事故が起きたため）。**収益が見込めるタイミングで専用ドメインを取得**してから有効化する方針（詳細は「組織サブドメインログイン機能」セクション）
- HOMEBASE（別リポジトリ`baseball-saas`）のサブドメイン対応は今回未着手のまま
- 通知（FCM）まわりの根本原因調査は前回セッションから引き続き未解決の可能性あり（下記「通知まわりの引き継ぎ状況」セクション参照、Antigravityとの相談も検討対象）

---

## 🛠️ 技術スタック & アーキテクチャ
- **フロントエンド**: React 19 + Vite + Lucide Icons + モダンCSS
- **バックエンド**: Cloudflare Workers (TypeScript / JavaScript REST API) & ローカルExpress/SQLite
- **クラウドDB**: Cloudflare D1 (サーバーレス分散SQLite)
- **ホスティング**: Cloudflare Workers（静的アセット配信＋API統合、2026-08-21〜。旧Cloudflare Pagesは廃止済み）

---

## 📂 主要ディレクトリ・コード構成
```text
telephone-message-app/
├── CLAUDE.md                     # Claude Code向け引き継ぎ・開発ガイド（本ファイル）
├── PROJECT_CONTEXT.md            # プロジェクト詳細仕様・APIエンドポイント定義
├── schema.sql                    # Cloudflare D1 データベーススキーマ
├── backend/
│   ├── worker.js                 # Cloudflare Workers API エンドポイント (D1接続)
│   └── index.js                  # ローカル開発用 Express + SQLite サーバー
├── frontend/
│   ├── src/
│   │   ├── App.jsx               # スイート統括メインコンポーネント (共通ステート・ルーティング)
│   │   ├── App.css               # スイート全体・チャット・CallSyncスタイリング
│   │   ├── main.jsx              # エントリーポイント
│   │   └── components/
│   │       ├── AppHeader.jsx     # 🌐 共通スイートヘッダー (アプリ切替・クイック受電登録・アカウント切替)
│   │       ├── ChatApp.jsx       # 💬 社内チャット専用アプリ (チャンネル・DM・スレッド)
│   │       ├── ChatArea.jsx      # 💬 チャットタイムライン・メッセージ入力・受電カードレンダリング
│   │       ├── ThreadDrawer.jsx  # 💬 メッセージ・受電メモへのぶら下がりスレッド会話
│   │       ├── CallSyncApp.jsx   # 📞 電話連絡DX専用アプリ (ボード・モニター・現場モバイル・台帳)
│   │       ├── CallMemoCard.jsx  # 📞 受電メモカード (未対応/対応中/完了ステータス操作・折返し発信)
│   │       ├── DeskMonitorView.jsx # 🖥️ 事務員デスクモニター (大型画面向け受電監視ボード)
│   │       ├── MobileViewMode.jsx  # 📱 現場モバイルビュー (ドライバー・外出営業向け簡易カード)
│   │       ├── ContactsDirectoryModal.jsx # 📇 受電先台帳・取引先名簿モーダル
│   │       ├── NewCallMemoModal.jsx # ➕ 受電メモ新規登録モーダル
│   │       ├── NewGroupModal.jsx   # 👥 新規グループ・チャンネル作成モーダル
│   │       └── AdminModal.jsx      # ⚙️ 組織・ユーザー・部署マスター管理モーダル
│   └── package.json
└── アプリ起動.bat                # ワンクリックローカル開発起動バッチ
```

---

## ✨ 実装完了済みの主要機能
1. **🌐 共通スイートヘッダー（`AppHeader.jsx`）**:
   - Google Workspace / Microsoft 365 形式のアプリ切り替えタブ（💬 社内チャット ⇄ 📞 電話連絡）。
   - どこからでも押せる「＋ 受電メモ登録」クイックボタン、受電先台帳、組織管理メニュー。
   - 共通アカウント（ユーザー・部署）の一元管理とワンクリック切り替え。
   - 未読メッセージ数・未対応受電件数のリアルタイムバッジ表示。

2. **💬 社内チャットアプリ（`ChatApp.jsx`）**:
   - 部署別チャンネル（#全体連絡, #営業部, #配送課など）と個人ダイレクトメッセージ（DM）。
   - テキストメッセージ、メンション、スレッド返信機能。
   - **電話メモカード連携**: CallSyncで電話が入ると、対象者のDMやチャンネルに「📞 受電メモカード」が流れてチャット上でも把握・ワンクリックでステータス変更が可能。

3. **📞 電話連絡DXアプリ（`CallSyncApp.jsx`）**:
   - **📋 ボード一覧**: 未対応・対応中・完了・自分宛てフィルター付きの受電カード一覧＆キーワード検索。
   - **🖥️ 事務員デスクモニター**: 事務所の大型モニターで全体の入電・対応状況を俯瞰できるマルチカラム画面。
   - **📱 現場モバイルビュー**: 外出先・スマホで直感的に確認・ワンタップで「折り返し完了」できるUI。
   - **📇 受電先台帳**: よくある相手先、電話番号、担当者リストを管理。

4. **☁️ Cloudflare D1 クラウド同期**:
   - 全データがクラウドDBにリアルタイム保存され、複数端末・複数ユーザー間で同期。
5. **🔔 着信音・Push通知（2026-08-18追加）**:
   - ヘッダーの🔔ボタンで、新しい受電メモが入った瞬間にチャイム音＋ブラウザ通知。
   - Service Worker + VAPID Web Push対応で、タブを閉じていても通知が届く（`frontend/public/sw.js`, `frontend/src/utils/push.js`, backend `/api/push/*`）。
6. **📱 モバイル対応（2026-08-18追加）**:
   - スマホ幅（〜640px）で社内チャットが「一覧⇄会話」を切り替える1カラム表示に。ヘッダーも圧縮される。

---

## 💻 開発・ビルド・デプロイコマンド
```bash
# フロントエンド開発ディレクトリへ移動
cd frontend

# ローカル開発サーバー起動 (http://localhost:5173)
npm run dev

# プロダクションビルド
npm run build

# 本番デプロイ（2026-08-21〜: フロント+バックエンドを統合したWorkerへ。リポジトリルートで実行）
cd ..
npx wrangler deploy

# ⚠️ 以下は廃止済み。もう実行しないこと（詳細は「Pages廃止」セクション参照）
# npx wrangler pages deploy dist --project-name callsync-app
```

---

## ✅ 2026-08-18 セッションでの完了事項
Claude Codeによるコードレビュー＋大規模な不具合修正・デザイン刷新を実施済み。詳細はgit logを参照。
- **重大バグ修正**: ステータス文字列の不一致（未対応バッジが常に0件だった）／ボード画面のデータ変換漏れ（カードが空欄）／**4つのモーダル（受電メモ登録・新規グループ・台帳・組織管理）が`isOpen`プロパティ未接続で全て開かなかった**不具合
- 未使用の旧デザインコード（Dashboard/Sidebar/MobileView/HistoryView/MasterManagement.jsx, index.css）を削除
- デザイン刷新（親しみやすさ重視の方向、ティール×グリーン基調に統一）、未定義だったCSSクラスを約50個追加
- 🔔 着信音・ブラウザ内通知・Web Push（Service Worker）を実装
- 📱 スマホ表示の崩れを修正（試みたが未解決、下記参照）
- 本番（callsync-app.pages.dev / callsync-backend）へデプロイ済み

## ⚠️ 未解決：スマホ表示（最優先で確認すること）
2026-08-18〜19に2回、はみ出し（横スクロール）が起きるバグを見つけて直した（ヘッダー圧縮・チャット一覧⇄会話の1カラム化・デスクモニターのカード幅・スレッドパネルの全画面化）。修正のたびに `mcp__Claude_Browser__resize_window` で375x812に変えて `getBoundingClientRect()` で要素のはみ出しを機械的にチェックし、はみ出しはゼロになったことを確認して本番デプロイ済み。

**それでもオーナーが実機のスマホで見ると「まだだな…」とのこと。** この作業環境にはスクリーンショットを撮る手段がなく（`computer` の screenshot/zoom は毎回 "pane not displayed" で失敗する）、要素が画面に収まっているかどうかの機械的な確認しかできていない — つまり「詰まりすぎ」「文字が小さい」「タップしづらい」「要素の重なり」のような、はみ出し検知では拾えない見た目の悪さが残っている可能性が高い。

**次にこの作業をする人へ**: 憶測でCSSを触る前に、オーナーに実機のスクリーンショットを送ってもらうこと。「どの画面で」「具体的に何が変か」を聞くこと。それなしで直しても同じ堂々巡りになる可能性が高い。

## ✅ 2026-08-19 セッションでの完了事項
**Phase 1: マルチテナント化を実装・本番反映済み。**
- `migrations/0001_multi_tenant.sql`（追記のみ、DROP無し）を本番D1に適用: `organizations`テーブル新設＋主要6テーブル（users, departments, chat_groups, call_memos, caller_contacts, push_subscriptions）に`organization_id`列追加。既存データは全て`organization_id = 1`（デフォルト組織）に自動移行、データ損失なし
- `backend/worker.js`の全エンドポイントを組織スコープ対応に更新（`organization_id`はクエリパラメータ/リクエストボディから受け取り、未指定時はデフォルト1＝現行動作を維持。まだログインが無いためフロントエンドは変更していない）
- `schema.sql`（新規インストール用）も同じ構成に同期
- ローカルD1で検証してから本番D1・`callsync-backend`にデプロイ、本番APIで組織分離を確認済み
- **組織作成（サインアップ）フローは未実装**（Phase 1のスコープ外、合意通り）。新しい組織を増やす場合は今のところ手動でDBに`INSERT INTO organizations`する運用

**次にこの作業をする人へ**: Phase 2（ログイン機能）に進む前提が整いました。ログイン実装時に、認証後のセッションから`organization_id`を解決してAPIに渡す形に置き換えてください（今はクライアント自己申告＝誰でも偽装できる状態のままなのは意図的な暫定措置）。

## ✅ Phase 2: ログイン機能（実装済み・本番反映は一部保留）

**バックエンド（実装・ローカル検証・コミット済み）**:
- `migrations/0002_auth.sql`: `sessions`テーブル新設、`users.password_hash`列追加（PBKDF2-SHA256, 100000 iterations, `salt:hash`形式）。既存6シードユーザーに初期PINをbootstrap済み（平文PINはオーナーにチャットで一度だけ報告済み、ファイルには残していない）
- `worker.js`に`/api/auth/login`（email+PIN）・`/api/auth/google`（Google ID tokenをtokeninfoエンドポイントで検証）・`/api/auth/logout`・`/api/auth/change-pin`（本人用）・`/api/auth/reset-pin`（管理者専用、同一組織内のみ）を追加
- **既存の業務系エンドポイント（users/departments/groups/contacts/call-memos/messages）はまだセッション必須にしていない**（意図的：フロントのログイン画面が無い状態でセッション必須にすると即座に全ユーザーが使えなくなるため、段階的に移行）
- Google側は`env.GOOGLE_CLIENT_ID`が未設定なら`/api/auth/google`は500を返すガード付き。クライアントID発行はオーナー作業中（Google Cloud Consoleで「Connect Suite」プロジェクト作成済み、OAuth同意画面設定中）

**フロントエンド（実装・ローカルで実際にログイン→ログアウト→リロード後の永続化まで動作確認済み）**:
- `LoginScreen.jsx`新設：ID（＝emailカラム、実メールである必要はない設計）＋PIN、Google Sign-Inボタン（`VITE_GOOGLE_CLIENT_ID`未設定時は非表示）
- `App.jsx`：`auth`が無ければ`LoginScreen`を表示するゲートを追加。`currentUserId`はログインユーザーのidに置き換え。主要な一覧取得・作成系リクエストに`organization_id`を付与（Phase 1のマルチテナント分離がフロントからも機能するように）
- `AppHeader.jsx`：なりすまし可能だったアカウント切替ドロップダウンを、現在ユーザー名の表示＋ログアウトボタンに置き換え済み

**⚠️ 本番デプロイ未実施（次にやること）**:
1. `npx wrangler d1 execute callsync-db --remote --file=migrations/0002_auth.sql`（このPCではPowerShellの実行ポリシーで`npx`が直接動かないため`cmd /c "..."`でラップする必要があった）
2. `npx wrangler deploy`（backend）
3. `cd frontend && npm run build && npx wrangler pages deploy dist --project-name callsync-app`（frontendのデプロイコマンドは把握しているが、このセッションではまだ実行していない——Phase 1の時と違い、ログイン画面が有効になるとオーナー自身がPINを知らないと入れなくなるため、デプロイ前にPIN一覧を確実に把握してもらってから進めること）

**Googleクライアント発行待ちの間、先に進められること**: 上記デプロイと、`VITE_GOOGLE_CLIENT_ID`（frontend）・`GOOGLE_CLIENT_ID`（wrangler secret/vars、backend）の設定。クライアントID到着後にこの2箇所へ設定し再デプロイすればGoogleログインも有効になる。

**⚠️ ネイティブアプリ化（Android/iOS）は「いつかやる」ではなく決定事項（2026-08-19、オーナー明言）**。HOMEBASEと同様にネイティブアプリとして配布する前提で進める。
- 現状（2026-08-19時点）はCloudflare Pagesの**Webアプリ（PWA）のみ**。Capacitor等によるネイティブラップ、パッケージ名/Bundle ID決定、署名鍵の準備は未着手
- Google OAuthは今回「ウェブ アプリケーション」のクライアントIDのみ作成する（Web版のログイン・バックエンドでのIDトークン検証の両方に必要な土台）。Android/iOS用クライアントIDは、実際にネイティブアプリの器（署名証明書のSHA-1フィンガープリント／Bundle ID）ができてから追加作成する — 後回しではなく技術的な前提条件（値が存在しないと作成できない）
- **次にこの作業をする人へ**: ネイティブアプリ化フェーズに入ったら、HOMEBASEの`HomebaseConnectionService`やCodemagicワークフロー（[timeline-worklog-app](../timeline-worklog-app)にも移植済み）を参考に、同じ構成をConnect Suiteにも適用できる

## 🗓️ 次回作業予定（2026-08-19時点でオーナーと合意済み・PC03等どのPCでも継続可）

**合意した方針・順番**: マルチテナント化を先に実装 → その上にログイン機能を乗せる（逆順だとユーザーテーブルを作り直す手戻りが発生するため）。

**ログイン機能の要件（2026-08-19 最終決定・ハイブリッド構成）**:
- **「Googleでログイン」（OAuth）と「ID＋PIN」の両方に対応する。** 一度「PINは作らない」で合意しかけたが、オーナーが復活を要望したため確定。理由: 自社(約60名、Android中心)は全員Googleアカウントを持っているので困らないが、**将来他社に売ることを考えると、Googleアカウントを持たない・使いたくない利用者がいる会社は普通にあり得る**ため、ID+PINの選択肢も必須という判断（Google Sign-In自体はiPhone/Safariでも動作する点はオーナーに伝達済みだが、それとは別の「販売先の多様性」が本質的な理由）。
- ログイン画面には「Googleでログイン」ボタンと「ID・PINでログイン」フォームの両方を用意する
- ID+PIN側: `users.password` → `password_hash` に置き換え、PINレベルの短い数字でOK（ただしハッシュ化はする。CloudflareWorkersはbcrypt非対応なのでWeb CryptoのPBKDF2等を使う）。本人がPIN変更／管理者がリセットできるようにする
- Google側: Google OAuth（Authorization Code Flow、またはGoogle Identity ServicesのSign In With Googleボタン）→ 検証済みメールアドレスを受け取る → `users.email` と突き合わせて本人特定
- どちらの方式でログインしても同じ `users.email` に一致させ、同一人物として扱う
- ログインIDは既存の `email` 列をそのまま使う（Google/ID+PIN共通）
- 新エンドポイント: `POST /api/auth/google`（Google IDトークン検証）, `POST /api/auth/login`（email+PIN）, `POST /api/auth/change-pin`（本人用）, `POST /api/auth/reset-pin`（管理者専用）, `POST /api/auth/logout`
- 今の「ヘッダーのアカウント切替ドロップダウン」（誰でも他人になりすませる作り）は両ログイン方式併用のログイン/ログアウトに置き換える
- 各API呼び出しの `user_id`/`created_by` は現状クライアントが自己申告した値をそのまま信用しており誰でも偽装できる。ログイン後はサーバー側でセッショントークンから本人を特定する形に直す

**将来の収益化モデル（オーナー確定事項）**: 1社＝1テナント（完全にクローズドな環境）を売る形。購入した会社のオーナー（管理者）が最初のアカウントを作り、そこから社員を追加していく、標準的なB2B SaaSの広がり方。**Googleログインは他社に売るときもそのまま使える**（各社が既に持っているGoogleアカウントで完結する）。マルチテナント化とセットで、**ログインしてきたユーザーのメールアドレスのドメイン（@より後ろ）を見て、どのorganizationに属するかを自動判定する**仕組みにする（Slack等の「会社ドメインでワークスペースに参加」と同じ考え方）。

**オーナー確認済みの背景（プロダクト設計の参考に）**:
- 全社導入した場合の規模は約60名。Google Chat（無料版）からの脱却を考えた決め手は「グループ作成に承認が必要で、その通知が分かりにくく気づかれない→結局口頭で説明する羽目になる」「新規メンバーが検索に出てこないと相談されるが原因不明で時間を取られる」の2つ。有料版なら解決するかもしれないが予算の余裕がない。
- CallSyncは構造的にこの2つの問題が起きない（グループ作成に承認ステップがない／3秒ごとの全体再取得なので新規メンバーもすぐ検索に出る）ことは既にオーナーに伝達・確認済み。
- 他に導入しているツール: デスクネッツ（連携したいと言及あったが、優先度は低く「将来やりたいことリスト」扱いで合意）。
- **CallSyncは投機的な新規事業ではなく、まずオーナー自身の会社の実運用課題を解決するために作っている。** 実際の一号ユーザー・パイロット先はオーナーの会社そのもの。他社への販売は本当に使えるようになった後で考える、という優先順位。

### Phase 1: マルチテナント化
- `organizations` テーブル新設（id, name, created_at）
- 主要テーブル（users, departments, chat_groups, call_memos, caller_contacts, push_subscriptions）に `organization_id` 列を追加
- 既存データは `organization_id = 1` として扱う（後方互換の移行）
- `backend/worker.js` の全クエリに `WHERE organization_id = ?` を通す
- 組織作成（サインアップ）フローはこのPhaseのスコープに含めるか要相談。最初は手動でDBに組織を作る運用でも可

### Phase 2: ログイン機能（Google OAuth）
- Google Cloud ConsoleでOAuthクライアントID発行が必要（オーナー作業）
- `users.password` 列（現状 平文で `password123` 等が入っている）は不要になるので削除、または無視して放置
- セッション管理方式を決める（`sessions` テーブル方式 or 署名付きトークン方式）
- 新エンドポイント: `POST /api/auth/google`（GoogleのIDトークンを検証→メールアドレスでorganization内のuserと突き合わせ→セッショントークン発行）, `POST /api/auth/logout`
- 新規メンバーがGoogleでログインしてきたが `users` テーブルに未登録の場合の扱いを決める（自動作成 or 管理者の事前登録必須にするか）
- フロント: ログイン画面（Googleログインボタン）を新設。ヘッダーのアカウント切替ドロップダウンを廃止しログアウトボタンに置き換え
- 全fetchにセッショントークンを付与し、バックエンド側で `user_id` をトークンから解決する形に変更（今はクライアントの自己申告を丸ごと信用しており誰でも偽装できる）

### まだ手を付けていない他の論点（過去レビューより）
1. **受電データの分析・レポート機能**: 「誰宛てへの入電が多いか」「どの取引先からの入電が多いか」「平均対応時間」などを可視化する分析ダッシュボード。
2. **CTI（電話着信連携）や外部連携**: クラウドPBX（Twilio, Zoom Phone等）との着信ポップアップ連携の可能性。

---

## ✅ 2026-08-19 セッション後半：デザイン刷新 + 事業方針の壁打ち

**Phase 1（マルチテナント化）・Phase 2（ログイン）は本番反映まで完了済み**（詳細は上記）。Google OAuthクライアントIDも発行済み・設定済み（`wrangler.jsonc`の`vars.GOOGLE_CLIENT_ID`、`frontend/.env`の`VITE_GOOGLE_CLIENT_ID`）。ネイティブアプリ（Android/iOS）化はオーナーが前提として明言済みの決定事項（今すぐではないが「いつかやる」ではなく確定方針、上記参照）。

**デザインを「白ベース＋やさしいパステル」に刷新（本番反映済み）**:
- ヘッダー・サイドバーは白背景＋濃い文字（以前は濃い緑の帯だった。「白ベース」という要望に合わせて変更）
- 色は差し色として使用（ブランド＝セージグリーン、セカンダリ＝ラベンダー、CTA＝ピーチ）。コンポーネント内にハードコードされていたTailwind系の色（青・グレー等、約140箇所）も一括で置き換え済み
- **配色トーン選択機能**: ヘッダーの⚙️（歯車）から「セージ／ラベンダー／ミスト」の3プリセットを選べる。自由なカラーピッカーではなく、**全プリセットでコントラスト比を事前検証済み**（白文字比較で5:1以上）にしてあるのがポイント——「読めなくなるのはNG」という要望への対応
- 同じ⚙️メニュー内に**文字サイズ選択**（標準／大きめ1.15倍／特大1.3倍）も追加。`html`のfont-size(rem基準)を切り替えるだけで全体に効く実装。老眼配慮での追加要望
- 本文の文字色自体も濃くした（`--ink`系トークン、WCAG AAA基準の7:1以上に）。老眼配慮
- 管理者専用の「組織管理」ボタンは🏢(Building2)アイコンに変更、⚙️(表示設定)と役割を分離
- 設定（トーン・サイズ）はブラウザのlocalStorageに保存（ユーザーごと、組織共通ではない）

**実機で見つかった不具合と直し方（今後のモバイル対応で同じ轍を踏まないためのメモ）**:
- `100vh`はモバイルChromeのアドレスバー分だけ実際より大きく計算される→`100dvh`を後段に書いて上書き（フォールバックとして`100vh`は残す）。**固定サイズのブラウザ検証環境では再現しない**ため、実機スクショでしか気づけなかった
- チャットヘッダーの折り返し・はみ出しは、`min-width: 0`を親に入れていなかったのが原因（flexボックスでellipsis省略が効かない典型パターン）
- ログイン直後に最初のグループチャットを自動的に開く挙動（8/18実装、設計意図の記録なし）は、モバイルの1カラム表示だと「勝手に画面遷移した」ように見えて不評だったため削除。PC・スマホともランディングは中立な空状態に統一

**事業方針の壁打ち（2026-08-19、まだ実行決定ではなく方向性の話）**:
- オーナーの仮説：①有料チャットを導入していない中小零細企業向けに「電話メモ＋おまけの社内チャット」を売る、②すでにChatwork/LINE WORKS/Slack等を導入済みの企業には、チャットを自前で作らず**Webhook連携**（受電メモをカードとして相手のチャットに自動投稿）で電話メモ機能だけ届ける、の二段構え
- Claudeの助言：②の「連携」は需要が確認できてから着手すべき（今は自前チャット版と連携版を同時に作るには開発リソースが足りない）。まず①の自社実運用＋知り合い数社への無料トライアルで反応を見て、実際に「Chatwork使ってるから連携さえあれば」という声が出てから連携開発に投資する、という順番を提案し、オーナーも合意
- 資金的な余裕がない状況（オーナー自己申告）を踏まえ、次の一手は機能追加ではなく「知り合いの中小企業2〜3社に無料で試してもらい、欲しいか・いくらなら払うか聞く」ことだと助言済み

**次にこの作業をする人へ**: 電話メモ（CallSync）側の作り込みが次の主題（オーナー予告）。「nonbaPC」（別PC）での継続作業を予定しており、本セッションの変更はすべてGitHubにpush済み（`git pull`で追従可能）。

---

## ✅ 2026-08-20 セッション：nonbaPCでの前進 + PC03で追跡・修正

**このセッションを引き継ぐ人（Claude Code / Antigravity 問わず）へ**: 作業はPC03とnonbaPCを行き来しながら進んでいます。**どちらで再開する場合も、まず`git pull`してこのファイルを読んでください。** 本セッションの変更はすべてGitHubにpush済みです。

**nonbaPCが昨夜（2026-08-19深夜〜20日未明）に進めた内容**（コミット履歴参照: `d6936b3`〜`0e51895`）:
- **Androidネイティブアプリ化（Capacitor）**: `frontend/android`・`frontend/ios`を新設。オーナーが決定事項として明言していたネイティブ化の第一歩
- **ファイル添付機能**: チャットで写真・PDFをキャプション付きで送れるように（R2バケット`callsync-attachments`を利用、`migrations/0003_attachments.sql`）
- **FCMネイティブPush通知の実装**: 「CapacitorのWebView内ではWeb Pushが不安定」という判断から、Android/iOSともにFirebase Cloud Messagingへ切り替え。`migrations/0004_fcm_tokens.sql`、バックエンドに手書きのFCM HTTP v1クライアント（RS256 JWT自己署名、Workersはfirebase-admin SDK非対応のため）。Web版は引き続き旧来のWeb Push（VAPID）を使用、ネイティブ版とは完全に別経路
- `codemagic.yaml`によるCI構築、iOSのビルド番号衝突などを個別に修正、`v0.1.0`・`v0.2.3`タグでリリースビルドをトリガー

**PC03で今日やったこと**:
1. **Android実機での通知不具合を調査**: 本人は「アプリ」（ネイティブ版）で試していたが`fcm_tokens`テーブルが空 → FCM登録が一度も成功していなかったと判明。あわせて、権限拒否時に「通知ON」と表示だけしてしまう不具合（Web版・ネイティブ版の両方）を発見・修正（`onToggleNotify`、`utils/nativePush.js`）。**ただしこの修正はWeb版には反映済みだが、スマホの既存インストール分に届けるには、nonbaPC側でCodemagicの新規ビルド＆配信（Google Play内部テスト）が必要**（PC03からはCodemagic/Play Consoleへのアクセス手段がない）
2. **本番で発生していた重大バグを発見・修正**: ログイン画面が**真っ白にクラッシュ**していた（Googleサインインボタンのdivに、Reactが管理する「読み込み中」の子要素を同居させていたため、Google側のDOM書き換えとReactのreconcileが衝突・`removeChild`エラー）。今日の他の作業とは無関係、本番で実際に起きていた。修正済み・デプロイ済み
3. **PIN管理UIの欠落を発見・修正**: 管理者の新規アカウント登録フォームが、Phase 2認証実装前のまま（メール必須・PINを設定する手段なし）だった。ID欄の文字列自由化、新規登録時の初期PIN欄、管理者のPINリセットボタン、本人用の「PINを変更」モーダルを追加
4. **CSVインポート機能を追加**: 管理者パネルからCSVで複数アカウントを一括登録できるように（列順: 氏名,ID,部門,権限,初期PIN固定。存在しない部門は自動作成、プレビュー→実行→結果表示の流れ）
5. **GoogleログインとID欄の関係を明確化**: ID欄に実在するGoogleアカウントのメールを入れればGoogleログインも使える、という説明をフォームに追記

**⚠️ 未確認事項（次にこの作業をする人へ）**:
- Android実機の通知ブロックが直ったかどうか、オーナーからの報告待ち（本人には「設定→アプリ→通知」を確認してもらうよう依頼済み）
- nonbaPC側でCodemagicの新規ビルド配信（v0.2.3以降のFCM対応版）が実際に完了しているか未確認。オーナーにも「私からはCodemagic/Play Consoleを確認できない」と伝達済み
- CSVインポート機能は本番でオーナー自身のテストがまだ（ロジックはローカルD1でend-to-end検証済み）

---

## ✅ 2026-08-20 セッション後半：オーナー/管理者/一般の権限階層 + HOMEBASE状況確認

**背景**: オーナーが将来の契約・プラン管理画面を見据えて「オーナー(契約・支払い)→管理者(オーナー指名、ユーザー/部門作成)→一般ユーザー(作成時に役割付き)」という3層モデルを提案。**プラン・支払い管理そのものはHOMEBASE側の会費ペイ実装（今週末予定、Stripeではなく会費ペイに切替済み・審査は2026-08-18通過）を待ってから着手する方針**（HOMEBASEと同じ課題を別々に解決する二度手間を避けるため）。役割階層(権限まわり)はお金と無関係なので先行実装。

**実装内容（本番反映済み・migrations/0005_owner_role.sql適用済み）**:
- `users.role`に`'owner'`を追加（スキーマ変更不要、元々自由文字列のTEXT列）。組織1の最初の管理者(id=1、佐藤健太)を`owner`に昇格済み
- **`POST/PUT/DELETE /api/users`・`/api/departments`に、本物のセッションベース認可を追加**（これまでクライアント自己申告のuser_id/organization_idを信用するだけだった長年の既知の欠陥——CLAUDE.md内で繰り返し「後で直す」と書かれていた箇所——を、この機能と合わせて解消）
  - 未ログイン・一般ユーザーは403
  - `role: 'owner'`の付与は、リクエスト元が`owner`本人でない限り403（管理者はオーナーを任命できない）
  - 組織内の最後の1人のオーナーは降格・削除ともにブロック（誤ロックアウト防止）
- フロント: ロール選択肢に「オーナー」はオーナー本人だけに表示。オーナーは一覧から削除ボタンが出ない。保存・削除系のfetchすべてにセッショントークンを付与し、403時にアラート表示
- ローカルD1で全パターン(未認証拒否／一般ユーザー拒否／管理者はオーナー付与不可／オーナーは何でもできるが自己ロックアウトはできない)を検証済み、本番でもオーナーラベル・組織管理ボタン表示を確認済み

**HOMEBASE側の状況確認（別リポジトリ`baseball-saas`、ついでに実施）**:
- 27日前の記憶(「プラン別ストレージ未実装」)は古く、現在は`src/lib/billing.ts`が存在し会費ペイ切替の土台はある。ただし**中身はまだStripeの価格ID前提の設計のまま**（会費ペイ実装時に書き直しが必要、`HOMEBASE_PROJECT_CONTEXT.md`に注意点を追記・push済み）
- また`getStandardStorageBytes()`（プラン別容量）は関数だけあり、まだチーム作成・プラン変更のどこからも呼ばれていない
- 作業中、PC03の`baseball-saas`ローカルに未コミットの重複変更（iOS申請準備、内容はすでにpush済みのコミットと完全一致）があり、安全に整理してpush済み

**次にこの作業をする人へ**: プラン・支払い管理（Connect Suite側）は、HOMEBASEの会費ペイ実装が終わってから、同じ設計を流用する形で着手する。それまでは今回作った役割階層(owner/admin/user)を土台に、CSVインポートなど周辺機能を先に固めておくのが良さそう。

---

## ✅ 2026-08-20 セッション終盤：新規登録導線 + プラットフォーム管理者ページ + 容量/プラン土台

**新規登録（サインアップ）フロー実装（本番反映済み）**:
- 今まで組織は手動でDBに作るしかなかった（「オーナーが契約→環境を作る→運用」の入口が存在しなかった）ことに気づき、`POST /api/auth/signup`を新設
- 組織名・オーナー氏名・ID・PINを入力するだけで、新しい`organizations`行＋`role='owner'`のユーザーが同時に作られ、そのまま自動ログイン
- 同一IDの重複は409で拒否（ユーザー作成失敗時は組織行もロールバックし、空の組織が残らないようにしてある）
- ログイン画面に「組織を新しく登録する方はこちら」のリンクを追加、双方向に行き来できる
- ローカル・本番の両方で、新組織のデータが既存組織から完全に分離されていることを確認済み

**プラットフォーム管理者ページ（`/platform-admin`、本番反映済み）**:
- 各組織のオーナー権限とは**完全に別の認証システム**（`platform_admins`・`platform_sessions`という専用テーブル、専用ログインエンドポイント）。組織のオーナーが誤って他組織を見られる経路が構造的に存在しない設計
- 表示内容は件数・容量などの運用状況のみ（メッセージ・受電メモの中身は一切見えない）
- 「解約」は自動削除しない方針（オーナー明確に確認済み）。データは残したままログインだけブロックし、「再開」で戻せる。「完全削除」は解約済みの組織にしかできず、かつ組織名を正確に入力しないと実行されない二段階の安全策
- ブートストラップ用のプラットフォーム管理者アカウントを1つ作成済み（`nonba30@gmail.com`、パスワードはチャット上でのみ共有・どこにも保存していない）
- ストレージ使用量（添付ファイルの合計サイズ、`messages.attachment_size`から計算）も表示・上限変更が可能。`organizations.plan_tier`・`storage_limit_bytes`という列を追加し、**金額・プラン名は未確定のまま、後からの手戻りを避けるための"器"だけ先に用意**（HOMEBASEの反省を踏まえた判断）

**次にこの作業をする人へ（他アプリで同様のページが欲しい場合）**: オーナーに伝えた依頼の型 = ①誰のためか ②見せない境界線(中身は見せない) ③ログインを別にする ④取れる操作/取れない操作(自動削除禁止など) ⑤将来の拡張(容量管理等)を先に言う、の5点を伝えてもらえば同じ考え方で作れる。

---

## 🔔 通知まわりの引き継ぎ状況（nonbaPCでの次回作業・Antigravityにも相談予定）

**発生している問題**: オーナーがAndroid**ネイティブアプリ**（Capacitorビルド）で通知を試したが、許可ポップアップも出ず、通知も届かない。

**このセッションで分かったこと（時系列）**:
1. `fcm_tokens`テーブルが空 → FCM登録が一度も成功していない
2. `push_subscriptions`（Web版のWeb Push、旧経路）には3件データがあった → これは開発中のテストで作られたもので、オーナー本人の今回のテストとは無関係と判明（オーナーは「アプリ」＝ネイティブ版で試したと後から判明）
3. コード上、権限が拒否された場合に「通知ON」と表示だけしてしまい、実際には何も登録されていない、という**サイレント失敗のバグ**をWeb版・ネイティブ版の両方で発見・修正済み（`onToggleNotify`、`utils/nativePush.js`）。この修正はWeb版には反映済みだが、**スマホの既存インストール分に届けるには、nonbaPC側でCodemagicの新規ビルド＆配信が必要**（PC03からはCodemagic/Play Consoleへのアクセス手段がない）
4. 「許可ポップアップが出ない」の最有力説：Androidは一度「拒否」された相手には二度とポップアップを出さないため、**端末の「設定→アプリ→通知」から手動で許可し直す必要がある**とオーナーに案内済み（結果はまだ未報告）
5. オーナーが使っているビルドは`386b8c3`（FCM関連コミットの後）のはずとのことで、コード的にはFCM対応は入っているはずだが、実際にどのビルド（デバッグ/リリース、どのタグ）が端末に入っているかは未確認

**まだ検証していない・分からないこと**:
- Android 13+の`POST_NOTIFICATIONS`ランタイム権限が、AndroidManifest.xmlに正しく宣言されているか（宣言漏れだと権限リクエスト自体が機能しない）
- `google-services.json`のパッケージ名・SHA-1証明書フィンガープリントが、実際にビルドで使われている署名鍵と一致しているか（不一致だとFCM登録がサイレントに失敗する典型パターン）
- Codemagicで`v0.2.3`以降のビルドが実際に成功し、Google Play内部テストに配信されているか
- 修正後のコード（サイレント失敗バグ修正版）を積んだ新しいビルドで、実機テストがまだ行われていない

**次にやること**: nonbaPCで、実機のログ（`chrome://inspect`でCapacitor WebViewをUSBデバッグ接続するか、Android StudioのLogcat）を見ながら、`PushNotifications.register()`が実際に`registration`イベントと`registrationError`イベントのどちらを発火しているか確認するのが一番早い切り分け方法。Antigravityに相談する場合も、上記の時系列と未検証事項をそのまま渡せば状況が伝わるはず。

---

## ✅ 2026-08-20 セッション最終：組織オーナー向けプラン・容量タブ + 新規ユーザーIDプレースホルダー修正

**背景**: オーナーから「オーナーの作り方まではいいけど、プラン（まだないけど）選びとかできるページは？」と指摘。プラットフォーム管理者側（`/platform-admin`）には`plan_tier`・`storage_limit_bytes`の管理機能を作ったが、**組織側のオーナー自身が自分の組織のプラン・容量を見る場所が存在しなかった**ことに気づき、閲覧専用のページを追加。あわせて「ユーザー作る時にID（アドレス）の欄に裏書きしてある文字が途中で切れてて何が書いてあるのかわかんない」という具体的なUIバグ報告も対応。

**実装内容（本番反映済み・新規マイグレーション不要、既存の`0007_storage_limit.sql`の列をそのまま利用）**:
- `GET /api/organization`を新設（`worker.js`）。`STAFF_ROLES`（owner/admin）のセッション必須、自分の組織のデータのみ返す（`session.organization_id`で絞り込み、他組織は見えない構造）。返す内容: `plan_tier`・`storage_limit_bytes`・`storage_used_bytes`（`messages.attachment_size`の合計、プラットフォーム管理者側と同じ計算式）・`user_count`
- `AdminModal.jsx`に3つ目のタブ「💳 プラン・容量」を追加（`PlanTab`コンポーネント）。現在のプラン名（今は`trial`のみ）、ストレージ使用量のプログレスバー（80%以上で警告色）、ユーザー数を表示。**プラン変更・アップグレードのUIはまだ無し**——「正式な料金プランは現在準備中です」という案内文のみ。これはHOMEBASEの会費ペイ実装が終わってから、同じ設計を流用して着手する方針（このCLAUDE.md内で繰り返し明記している通り）
- 新規ユーザー作成フォームのID欄プレースホルダーが、グリッドの半幅カラムに収まらず途中で切れて読めなくなっていたバグを修正。長い説明文（"ID+PINのみなら実在しなくてもOK"）はプレースホルダーから外し、`例: yamada@example.com`という短い例に変更。元の説明は欄の下のヒントテキストに統合し、Googleログインを使わせたい場合は本人の実アドレスが必要である旨も明記

**検証**: ローカルD1でcurl検証済み（認証あり→正しいJSON、認証なし→403）。本番デプロイ後、`/api/organization`が本番でも同じ403ガードで応答すること、本番配信されているJSバンドルが最新ビルド（`index-DgMaesCW.js`）であることを確認済み。実際のオーナーアカウントでのブラウザ上の見た目確認は未実施（本番の実オーナー認証情報を持っていないため）——**次にログインした際に「歯車→プラン・容量」タブの表示と、新規ユーザー作成画面のID欄プレースホルダーが崩れていないか、見た目だけ確認してほしい**。

**次にこの作業をする人へ**: プラン選択・アップグレードの実UIと決済連携は、HOMEBASE側の会費ペイ実装（今週末予定）が終わってから着手。それまでは今回作った閲覧専用ページがそのまま土台になる（`GET /api/organization`のレスポンス形はプラン選択UIでもそのまま使い回せるはず）。

---

## ✅ 2026-08-20 セッション追加分：ID欄レイアウト再修正 + オーナー権限の選択不可化 + 組織番号プレフィックス方式のID + 初回ログイン強制PIN変更 + モバイルレイアウト崩れ修正

**背景**: 前回のID欄プレースホルダー修正だけでは不十分で、オーナーからスクショ付きで「まだ切れてる」と再指摘。あわせて3点の追加指摘: ①権限プルダウンで「オーナー」が選べてしまうのはおかしい、委譲は別プロセスにすべき ②IDはグローバルに一意（`users.email TEXT UNIQUE`、しかもログインは組織を問わずID+PINだけで引き当てる設計）なので、他社と同じIDを付けたら弾かれる・衝突する可能性がある ③管理者メニューのタブや新規登録ボタンが、モバイル幅で日本語ラベルが1文字ずつ変な位置で改行される見た目崩れがある。

**実装内容（本番デプロイ・本番D1マイグレーション適用済み、ブラウザで実機フロー確認済み）**:

1. **ID欄レイアウト**: `所属部門`と`ID`を横並び2カラムグリッドから外し、それぞれ独立した全幅の行に変更（`AdminModal.jsx`）。加えて後述の組織番号プレフィックス方式により、入力欄自体もより短い文字列で済むようになった

2. **オーナー権限を選択肢から完全削除**: 新規作成・編集フォームの権限プルダウンから「オーナー」を除去（委譲は別プロセス、という指摘どおり今回は非対応のまま）。オーナー自身を編集する画面では、権限欄がロック表示（変更不可の静的ラベル）になる。**バックエンド側（`POST/PUT /api/users`）も、直接API越しでもオーナーの新規付与を常に403で拒否**するよう修正——UIを迂回されても同じ制約がかかる。既存オーナー自身のプロフィール編集（名前・部門・アバター）は、role: 'owner'のまま送っても通るよう区別して実装

3. **組織番号プレフィックス方式のID**（HOMEBASEと同じ考え方を踏襲）: `users.email`はDB全体でUNIQUE制約があり、ログインもID+PINのみ（組織選択なし）で引き当てる設計のため、**他組織のユーザーと同じIDを付けると衝突する**という指摘は正しかった。組織ごとに新しいテーブル列は増やさず、**組織ID自体を3桁ゼロ埋めした番号（例: 組織id=7なら`007`）をそのままIDの先頭プレフィックスとして使う**方式にした。新規アカウント作成フォームでは「007_」の固定ラベル＋自由入力欄という見た目にして、送信時にバックエンドではなくフロント側で結合（CSVインポートも同様に自動プレフィックス付与、CSVには自由入力部分だけ書けばよい）。**オーナー自身のID（実メールアドレス）は対象外**——実メールは元々グローバルに一意なので不要、かつ将来の請求連絡先として実アドレスのまま保つ必要がある。**既存アカウントは無変更**（新規作成時のみ適用、後方互換）

4. **初回ログイン時の強制PIN変更**: 「未入力なら後で管理者がリセットできます」という文言をやめ、挙動自体を変更。新規アカウントのPINが未入力なら`0000`をデフォルトにし、**管理者が知り得るPIN（初期PINまたは管理者によるリセット）は必ず本人に次回ログイン時の変更を強制する**仕様にした（`users.must_change_pin`列を追加、`migrations/0008_pin_policy.sql`）。本人が自分で選んだPIN（オーナー登録時の自己設定、または本人の「PINを変更」からの変更）は強制されない。ログイン・Googleログインのレスポンスに`must_change_pin`を含め、`App.jsx`側で真の場合は`ChangePinModal`を`forced`モード（閉じるボタン・キャンセルなし）で全画面表示し、変更完了までアプリ本体に入れないようにブロックする

5. **モバイルレイアウト崩れ修正**: 管理者メニューのタブバー（アカウント管理／部門マスタ管理／プラン・容量）が、モバイル幅で日本語ラベルが1文字だけ改行されるなどの崩れがあった。タブバーは`overflow-x: auto`にして横スクロール可能にし、各タブは`white-space: nowrap`で折り返さないよう変更。CSVインポート・新規アカウント登録ボタンの行も`flex-wrap`でボタン単位の折り返しに変更し、ボタン内テキストが割れないようにした。**電話メモ側や他のレイアウト崩れは今回は対象外**（オーナーの発言どおり、電話メモは別途これから着手する作業のため）

**検証**: ローカルD1・本番D1の両方にマイグレーション適用済み。ローカルでcurlによる一連のシナリオテスト（オーナー登録時のメール形式チェック、一般ユーザーへのオーナー付与拒否、既存オーナー自身の編集は許可、PIN未入力→0000デフォルト→must_change_pin=1→ログイン時にtrueで返る→本人の変更で0に戻る）を実施し全て想定通り。本番でも使い捨てのテスト組織を作成し、実際のブラウザ操作で「007_」プレフィックス表示、オーナー編集画面のロック表示、強制PIN変更画面（閉じられないこと）が正しく機能することを確認、モバイル幅（375px）でタブバーとボタン行が1行に収まる（折り返さない）ことも確認済み。テスト組織はプラットフォーム管理者側から解約→完全削除で後片付け済み。

**次にこの作業をする人へ**:
- IDのグローバル一意性そのもの（ログインに組織選択がない設計）は今回は変更していない。プレフィックス方式はあくまで「衝突を実質的に防ぐ」運用上の対策であり、本気で複数組織対応のログイン設計にするなら、将来的にはログイン時に組織を選ばせる／組織ごとのサブドメインにするなどの見直しも選択肢になる
- オーナー権限の委譲プロセスは未実装（今回はUI・APIともに完全に不可能にしただけ）。将来必要になったら、プラットフォーム管理者を経由する、または現オーナーの本人確認を伴う専用フローとして別途設計するのが良さそう
- 電話メモ側・その他画面のレイアウト崩れは今回スコープ外。次にそちら（nonbaPC予定）に着手する際は、今回と同じ「日本語ラベルはスペースがなく任意の位置で折り返されるので、`white-space: nowrap`＋`overflow-x: auto`か`flex-wrap`のどちらかで意図的に制御する」という考え方を踏襲すると良い

---

## ⚠️ 2026-08-20 セッション最終盤：組織サブドメインログイン機能を実装 → 本番事故（他アプリを巻き込んだ）→ ルート削除で復旧、ドメイン方針を再検討

**背景**: 上記の組織番号プレフィックス方式（`007_yamada`）について、オーナーから「これは本来やりたかった形（Slackのワークスペースみたいに会社ごとのURL）とは違う」と指摘。会社ごとのサブドメインログイン（`会社名.easystance.app`）を作ることに合意し、実装した。ドメインは新規取得ではなく、既存の`easystance.app`（他のアプリ`worklog.easystance.app`と共有中）を無料で流用する方針でスタート。

**実装内容（コードは本番反映済み・機能自体は正しく動作することを確認済み）**:
- `organizations.slug`列を追加（`migrations/0009_org_slug.sql`。SQLiteは`ALTER TABLE ADD COLUMN ... UNIQUE`ができないため、列追加とUNIQUE INDEX作成を分離）
- `GET /api/auth/check-slug`（空き状況チェック、予約語リストあり）、オーナー専用`POST /api/organization/slug`（既存組織が後からURLを設定できる）
- `worker.js`に`resolveOrgFromHost()`を追加。`Host`ヘッダーから組織を解決できた場合はログインをその組織にスコープし、解決できない場合（サブドメイン未設定の組織、`callsync-app.pages.dev`、`workers.dev`経由など）は**今まで通りのグローバル検索にフォールバック**——既存動作は一切変更していない
- **重要な設計判断（意図的に見送った部分）**: `users.email`のグローバルUNIQUE制約はそのまま。これを`UNIQUE(organization_id, email)`に緩めれば「別会社が同じID文字列を使える」という本来のゴールに近づくが、SQLite/D1でのテーブル再構築が必要な上、グローバルフォールバックが存在する間にこれをやると危険（2社が同じIDを持てるようになった瞬間、フォールバック側の検索が曖昧になる）。今回は見送り、別途の慎重な移行として扱う
- サインアップ画面に会社URL（任意）の入力欄＋空き状況のリアルタイム確認、登録完了後にURLを大きく表示。管理者メニューの「プラン・容量」タブに、既存組織が後からURLを設定できるカードを追加
- Cloudflare Pagesはワイルドカードカスタムドメインをサポートしていないことが判明したため（無料枠はもちろん有料枠でも非対応、コミュニティで継続議論中）、フロント＋バックエンドを1つのWorker（`callsync-backend`）に統合。`wrangler.jsonc`に`assets`バインディング（`not_found_handling: "single-page-application"`でSPAフォールバック）を追加し、`/api/*`以外は静的アセットとして配信するように変更。**既存の`callsync-app.pages.dev`（Pages）はそのまま残し、レガシーアクセス経路として維持**（置き換えていない）

**⚠️ 本番事故とその後**: サブドメインを実現するために`*.easystance.app/*`というワイルドカードルートをWorkerに追加したところ、**同じドメインを使っている別アプリ`worklog.easystance.app`（timeline-worklog-app、無関係な別プロジェクト）を一時的に乗っ取ってしまった**（`worklog.easystance.app`にアクセスするとConnect Suiteの画面が出る状態になった）。Cloudflareの公式ドキュメントでは「完全一致のルートがワイルドカードより優先される」とあったが、実際の挙動はそうならなかった（Pages カスタムドメイン vs Workers ワイルドカードルートの優先順位の実挙動がドキュメント通りではなかった、と考えられる）。

対応: `wrangler.jsonc`からルートを削除して`wrangler deploy`・`wrangler triggers deploy`を実行したが、**Cloudflare側に登録されたルートが実際には削除されず**、`worklog.easystance.app`が直らなかった。最終的にCloudflare API（`DELETE /zones/{zone_id}/workers/routes/{route_id}`）を直接叩いてルートを強制削除し、復旧を確認（`worklog.easystance.app`・`callsync-backend.nonba30.workers.dev`とも正常に戻ったことを確認済み）。

また、この作業の途中で別の設定ミスにも気づいて即座に修正済み: `wrangler.jsonc`に`routes`を追加すると、デフォルトの`workers_dev`ルート（`callsync-backend.nonba30.workers.dev`——フロント全体のAPI_BASEがハードコードで依存している本番の生命線URL）が**自動的に無効化される**Wranglerの仕様があり、一時的にAPI全体が404になっていた。`"workers_dev": true`を明示することで復旧（現在の`wrangler.jsonc`にはこの設定が入っている）。

**現在の状態**: サブドメインログイン機能のコード自体（`resolveOrgFromHost`、slug設定UI、check-slugなど）は本番に残っており、ロジックとしては正しく動作することを確認済み（別々の組織のサブドメインで、他組織の認証情報でログインできないことも確認済み）。**しかし実際にその機能を使うための`*.easystance.app/*`ルートは削除したまま**——つまり、オーナーが管理画面で組織URLを設定すること自体はできるが、実際にそのURLにアクセスしても今は何にも繋がらない状態。危険なので意図的にこのままにしてある。

**オーナーの決定**: この事故を踏まえ、`easystance.app`を共有し続ける対応（worklog専用の除外プロキシを足すなど）は採用せず、**「収益が見込める状況になったタイミングで、Connect Suite専用の新規ドメインを取得する」**という方針に決定。専用ドメインなら他アプリとの衝突が構造的に起こり得ないため、今回のような事故のリスクがそもそも存在しなくなる（Cloudflareの有料オプション「Advanced Certificate Manager」月$10で`*.connect-suite.easystance.app`のような2段階ワイルドカードを使う案より、専用ドメイン年数千円の方が安く、かつ安全）。メモ`subdomain-multitenancy-plan.md`にも記録済み。

**次にこの作業をする人へ**:
- **今すぐ`*.easystance.app/*`のようなワイルドカードルートを`easystance.app`ゾーンに追加しないこと**——同じアカウント上の他アプリを巻き込む実績があるので要注意。追加するなら、まずそのゾーンに登録されている既存の全ルート（`GET /zones/{zone_id}/workers/routes`で確認可能）を事前に確認すること
- Cloudflareの`routes`設定を`wrangler.jsonc`に追加する時は、必ず`"workers_dev": true`も明示すること（デフォルト無効化される仕様のため）
- ルートの削除は`wrangler deploy`だけでは反映されないことがある（今回`wrangler triggers deploy`も試したが同様に反映されず、結局Cloudflare API直叩きが必要だった）。ルート変更後は必ず影響範囲（自分のアプリ・共有ドメイン上の他アプリ両方）を実際にcurlして確認すること
- 専用ドメイン取得のタイミングになったら: ドメイン取得→そのゾーンをCloudflareに追加→`wrangler.jsonc`の`routes`をそのドメインに向ける→動作確認、という流れで良いはず。他アプリとの共有がなくなるので、今回のような事故の心配は不要になる

---

## ✅ 2026-08-20夜〜2026-08-21未明：nonbaPC + Antigravityでの作業（コミット13件、本番反映済み）

**背景**: このセッションの後、オーナーはnonbaPCに戻り、Antigravityと一緒に通知まわり・電話メモ（CallSync）の作業を進めた（`f029232`〜`748b8dd`、2026-08-20 21:33〜2026-08-21 01:12）。**この回はCLAUDE.mdが更新されないまま終わっていた**ため、次のセッションで`git log`とコミットメッセージからまとめて追記したもの。他PC・他ツールで作業した後は、たとえ時間切れでも一言でいいのでCLAUDE.mdに書き残す習慣を継続してほしい。

**やったこと（本番デプロイ済み、Workerのバンドルハッシュで確認済み）**:
- **通知**: チャットのWebプッシュ通知・アプリ内チャイム・未読バッジ・通知タップ時のディープリンク遷移を実装（`4019aa3`）。Androidネイティブプッシュのタップ時ナビゲーションにも対応（`78b1cc4`）
- **Capacitorのライブ更新化**（`748b8dd`）: `frontend/capacitor.config.ts`の`server.url`を本番Worker（`https://callsync-backend.nonba30.workers.dev`）に向ける設定に変更。**これによりWeb側の更新が、アプリの再ビルド・ストア再提出・端末での再インストールなしにそのまま反映されるようになった**（既存インストール分にも次回起動時に反映される想定）。今後のFCM関連の修正などもこの経路で届けられる
- **@メンション機能一式**（`b0fbd0d`, `af3efbb`, `ffebd2e`）: オートサジェスト、バッジpill表示、自分がメンションされた時のハイライト、メンション時の通知優先度アップ。グループ絵文字ピッカーのデザイン刷新、オーナー/管理者バッジの正確な表示
- **電話メモ（CallSync）の大幅強化**（`2bff75c`, `f029232`, `cacc8e5`, `78b1cc4`, `4b52552`）: 受電先タグ、対応コメントのインライン表示、デスク表示での対応先の柔軟な選択、受電日時・受信者・対象バッジの表示、グループのクイックテンプレート、メンションピル表示、部門/グループ宛メモへのメンション選択、DMディープリンク遷移先の修正、メモグループの所属フィルタ修正
- **モバイル対応の大改修**（`3657383`, `10f4f78`）: スマホなら現場向け表示・PCならデスクモニター表示に自動切替。管理者メニュー・CallSyncのサブナビ・現場向けヘッダーのモバイルレイアウト調整
- **`_redirects`ファイルを完全削除**（`ada5e95`）: 「wrangler SPA設定と競合するため」という理由で`frontend/public/_redirects`自体を削除。今回のセッションでWorker側は`not_found_handling: single-page-application`で同等の機能を持つため実害はないが、**もし今後`callsync-app.pages.dev`（レガシー経路のPages）を再デプロイすると、`_redirects`がないため`/platform-admin`など直接アクセスされるSPAルートが404になる可能性がある**。現時点でのPages側の最新デプロイ（`0f93a25`時点のビルド）はこの削除より前のものなので、今はまだ壊れていない

**次にこの作業をする人へ**:
- Pages（`callsync-app.pages.dev`）を今後再デプロイする予定があるなら、`_redirects`削除の影響（`/platform-admin`直アクセスの404化）を先に確認・対応してから行うこと。あるいは、Pages自体をもう使わない方針に倒すなら、レガシー経路として案内している記述をどこかで整理する
- Capacitorのライブ更新化により、今後ネイティブアプリの検証は「Web側をデプロイ→アプリを再起動」で完結するはずなので、Codemagicでの再ビルドが本当に必要なケース（プラグインのネイティブコード変更、`capacitor.config.ts`自体の変更など）と、不要なケース（JS/CSSの変更のみ）を区別して案内すると手間が減る

---

## 🗑️ 2026-08-21：Cloudflare Pages（`callsync-app.pages.dev`）を正式に廃止

**経緯**: 上記の`_redirects`削除の影響について、オーナーに「Pages自体もう使ってないのでは？」と確認したところ、実は**まだWeb版のブックマークとして`callsync-app.pages.dev`を使っている状態**だったことが判明（「まだ使ってるって程の状態ではないけど」とのこと）。中身は今のWorker配信と完全に同一（配信ファイルのハッシュも一致確認済み）なので、機能を失わずに乗り換え可能と判断し、正式にPagesを廃止する方針に決定。

**やったこと**:
- CLAUDE.md冒頭の「本番Webアプリ」の記載を、Worker側URL（`https://callsync-backend.nonba30.workers.dev`）に変更
- デプロイコマンドの案内から`wrangler pages deploy`を削除（廃止済みの注記を追加）

**オーナー・今後この作業をする人へのご案内**:
- **これからWeb版を開く時は `https://callsync-backend.nonba30.workers.dev` を使ってください**（ブックマークの差し替えをお願いします）
- 中身・機能は今までと完全に同じですが、**ドメインが変わるためログインは再度必要**になります（ブラウザの保存領域はドメインごとに独立しているため、旧URLでのログイン状態は新URLには引き継がれません）
- `callsync-app.pages.dev`自体は当面残りますが、**今後一切更新されません**（`wrangler pages deploy`は二度と実行しないでください）。中身が古いまま固定されるため、いずれ本体との差異が出ます
- 将来的に完全に削除（Cloudflareのプロジェクト自体を消す）してもよいが、今回は「触らず放置」にとどめている。積極的に消したい場合は`wrangler pages project delete callsync-app`が該当コマンド（未実行）

---

## 🌐 2026-08-21：固定サブドメイン `connectsuite.easystance.app` を追加（個人アカウント名を隠すため）

**背景**: オーナーから「`callsync-backend.nonba30.workers.dev`はURLに個人のCloudflareアカウント名（`nonba30`）が出てしまうので使いたくない」との指摘。あわせて、元々「`connectsuite.easystance.app`のようなアプリ全体の固定URLを1つ用意し、その下に契約ごとの部屋（会社サブドメイン）を並べる」という2段構えのプランだったことが判明——上のセクションの事故で分かった「2段階目のワイルドカード（`*.connectsuite.easystance.app`）は無料SSLでカバーされない」という制約のせいで、下の階層（会社ごとの部屋）だけ収益が見込めるまで保留、という位置づけ。今回はその上の階層（固定URL）を実現した。

**なぜ今回は安全か**: 前回の事故は「`*.easystance.app/*`」という**ワイルドカード**ルートが、ゾーン上の他の全サブドメイン（`worklog.easystance.app`など）を巻き込んでしまったのが原因。今回追加したのは`connectsuite.easystance.app/*`という**完全一致（ワイルドカードなし）**のルートなので、他のどのサブドメインにも一切影響しない。追加前後で`worklog.easystance.app`が無事であることを実機確認済み。

**実装**: `wrangler.jsonc`の`routes`に`{ "pattern": "connectsuite.easystance.app/*", "zone_name": "easystance.app" }`を追加し`wrangler deploy`。DNS側は、Cloudflare APIの権限（zone:read のみ、DNS書き込み権限なし）の都合でこちらでは追加できないため、オーナーにダッシュボードでのCNAMEレコード追加（`connectsuite` → `easystance.app`、プロキシON）をお願いした。

**次にこの作業をする人へ**:
- `easystance.app`ゾーンにルートを追加する時は、**必ず完全一致パターン（ワイルドカードなし）にすること**。ワイルドカード（`*.easystance.app/*`のような）は二度と追加しないこと（他アプリを巻き込む実績があるため）
- フロント側の`API_BASE`は今も`https://callsync-backend.nonba30.workers.dev/api`のまま（`connectsuite.easystance.app`も同じWorkerを指しているため機能的には問題ないが、ブラウザの開発者ツールでネットワークタブを見ると`nonba30`のドメインへの通信が見える。今回はアドレスバー・ブックマークに出ないことを優先し、ここまでは変更していない。完全に隠したい場合は`API_BASE`もこの新ドメインに揃える対応が別途必要）
- 会社ごとの部屋分け（`会社名.connectsuite.easystance.app`のような2段階目のワイルドカード）は、上のセクション通り「収益が見込めるタイミングで専用ドメイン取得」の方針のまま変更なし

---

## ✅ 2026-08-21：チャットの実使用フィードバック3件 + そこから見つかった関連バグ2件（本番反映済み）

**背景**: オーナーが実際にチャットを使ってみて気づいた3点を報告。

**対応内容**:
1. **1対1DMに@メンション不要**: `ChatArea.jsx`で`activeChat.type === 'dm'`の時は@メンションボタン・オートサジェストを非表示に。2人しかいない会話で相手を指名するのは冗長なので
2. **開いたままのチャットに新着が反映されない**（報告のあった一番の問題）: 原因は`App.jsx`の`fetchMessages()`が、チャットを開いた瞬間と自分が送信した時にしか呼ばれておらず、他のデータ（ユーザー一覧など）と違って3秒ごとのポーリング対象になっていなかったこと。`fetchAllData`と同じ3秒ポーリングパターンを、開いているチャットのメッセージ取得にも追加。実機（2タブでの実セッション）で、相手が送ったメッセージが自分の画面に何もせず数秒で反映されることを確認済み
3. **スマホでのEnter送信**: PCの「Shift+Enterで改行」はスマホには存在しないキーなので、複数行のメッセージが打てなかった。既存の`window.innerWidth < 768`判定（CallSyncの現場/デスク表示切替で使用中のものと同じ基準）を流用し、**モバイル幅ではEnterは常に改行、送信は送信ボタンのみ**に変更。PC幅では従来通りEnterで送信。両方の挙動を実機で個別に確認済み（PC: Enterで送信されテキスト欄が空になる／モバイル: Enterで`preventDefault`されずテキストが残る＝ネイティブ改行に委ねられる）

**この過程で見つけた本番バグ2件（いずれも修正・デプロイ済み）**:
- **組織の完全削除がDMのあるDM組織で失敗する**: プラットフォーム管理者の「完全削除」機能が、`messages`テーブルの削除条件を`target_type = 'group'`だけに絞っていて、DMメッセージを削除し忘れていた。DM履歴のある組織を完全削除しようとすると、ユーザー削除時に外部キー制約エラーで失敗（解約はできるが完全削除だけ失敗する状態）。`sender_id`基準の削除に変更し修正、DM・グループ両方のメッセージがある組織で削除できることを確認済み
- **チャットで発言したことのある社員アカウントを削除できない**: 管理者メニューの「社員アカウント削除」も同じ原因（`messages.sender_id`の外部キー）でエラーになっていた。チャット機能が実際に使われ始めた今日、初めて表面化した可能性が高い。**オーナーに確認の上、ハード削除ではなくソフトデリートに変更**（`users.deleted_at`列を追加、`migrations/0010_soft_delete_users.sql`）——退職者のアカウントを消しても、他のメンバーから見た会話履歴・送信者名はそのまま残る設計。削除された本人はログイン不可（PIN・Googleどちらも）、既存セッションも即座に無効化。ユーザー一覧（`GET /api/users`）からは除外される

**次にこの作業をする人へ**:
- ユーザー関連のクエリを新しく書く時は、`deleted_at IS NULL`条件を付け忘れないよう注意（アクティブ一覧・ログイン系はフィルタ必須、メッセージの送信者表示など「過去の記録として見せる」系はフィルタ不要、が使い分けの基準）
- 同様の「削除しようとしたら外部キー制約で落ちる」パターンが他のテーブルにも潜んでいないか、心当たりがあれば早めに洗い出すとよい（今回は偶然チャット実使用で発覚した）

---

## ✅ 2026-08-21：電話メモ専用通知欄を実装（本番反映済み）

**背景**: 電話メモがチャットのメッセージ一覧に混ざって表示される今の仕様について、オーナーから「電話連絡を埋もれさせない」というこの製品そもそもの存在意義と矛盾しかねない、という指摘。壁打ちの結果合意した方針をそのまま実装:

- チャットサイドバーの最上部（グループ・DM一覧より上）に`CallMemoTray.jsx`（新規ファイル）を追加。自分宛て（`target_type === 'dm' && target_id === 自分`）・自分の所属グループ宛て（`target_type === 'group' && 自分がメンバー`）の受電メモだけを新着順に表示。これが唯一の「電話メモの入り口」
- チャット内メッセージとして流れる受電メモカード（`CallMemoCard`）は変更なし、そのまま残している（会話の文脈用の軽量参照という位置づけ）
- 未対応（`status !== 'resolved'`）が1件でもあると、欄全体がバナー的に色付き強調表示（`has-unhandled`クラス、`--status-pending`系の色）＋件数バッジがパルスアニメーション。これは「新着に気づく」だけでなく「後から追いついて確認する」を両方満たす常設表示にした（ボタン単体だと見逃した後の確認手段が弱いという壁打ちの結論通り）
- ✓ボタンで即座に「対応済み」にできる

**実装上の細かい判断**: DM宛てメモの場合、`target_name`（APIが返すtarget側の名前）はDMの相手＝メモを受け取った本人の名前になってしまう（＝自分の名前が表示されて誰が記録してくれたか分からない）ため、DM宛ての場合だけ`creator_name`（記録した人）を表示するよう調整した。グループ宛ての場合は`target_name`（グループ名）をそのまま表示。

**検証**: 本番で使い捨てテスト組織を作成し、DM宛て・グループ宛てのメモそれぞれで表示内容・未対応バッジ・強調表示・対応済みボタン（押すとバッジが消える）すべてブラウザで実機確認済み。テスト組織は解約→完全削除で後片付け済み。

**次にこの作業をする人へ**: 既存の`callMemosCount`（ヘッダーの「N件 未対応」バッジ、`AppHeader.jsx`）は今回**あえて変更していない**——あちらは組織全体の未対応件数、今回作った専用欄は「自分宛て」に絞った件数で、意味が異なる。両方残す設計のままにしてあるが、将来「2箇所で数字が違って紛らわしい」という声が出たら、ヘッダー側も自分宛てに絞るか、どちらかに統合するかを検討すると良い。

---

## ✅ 2026-08-21（続報）：クリック時の挙動を「会話にジャンプ」→「専用欄内で詳細を開く」に修正

**背景**: 上記の初版では、各項目をクリックすると該当の会話（DMまたはグループ）にジャンプする仕様だった。実機で試したオーナーから「結局チャットに人を送り込むだけで、埋もれる問題の解決になってない。電話メモに飛べばいいじゃん」と的確な指摘。加えて、初版には別の実バグもあった：DM宛てメモの遷移先に`target_id`（＝メモを受け取った本人＝自分自身のID）を使っていたため、**「自分自身とのDM」という空っぽの会話に飛んでしまう**不具合があり（オーナーが実機のスクショで発見）、まずそれを`created_by`（記録してくれた人）に向ける修正をした直後に、そもそも「会話に飛ばす」という設計自体への指摘を受けた、という流れ。

**最終的な実装**: 会話への遷移を完全にやめ、**クリックした項目をその場（サイドバー内）でアコーディオン展開し、電話メモの詳細（会社名・担当者・電話番号・件名・本文・対応中/完了ボタン・対応メモ入力欄）をチャット画面に一切移動せず表示・操作できる**ようにした。既存の`CallMemoCard.jsx`（チャット内で使っているカード）は流用しなかった——`GET /api/messages`経由の`memo_*`プレフィックス付きフィールド名を前提にしている上、スレッドボタンがメッセージID（`GET /api/call-memos`には含まれない）を必要とするため、データ形状が合わなかった。代わりに、同じCSSクラス（`call-badge-tag`・`phone-pill-btn`・`memo-body-box`など）を再利用した専用の軽量詳細ビュー（`CallMemoTrayDetail`、同ファイル内）を新規作成。

**次にこの作業をする人へ**: 「チャット内カードとほぼ同じ見た目・機能を、データ取得元が違う2箇所（`GET /api/messages`経由と`GET /api/call-memos`経由）で別々に実装している」状態になっている。将来どちらかのAPIレスポンス形状を揃える、または共通のadapter関数を用意する形でリファクタリングする余地はあるが、今回は緊急度・影響範囲を考えて見送った。

---

## ✅ 2026-08-21：Android実機の「戻る」操作でアプリごと終了してしまう不具合を修正（要ネイティブ再ビルド）

**背景**: オーナーから、スマホの戻るジェスチャー/戻るボタンを押すと、画面が1つ前に戻るのではなく**アプリごと終了してしまう**との報告。原因は単純で、`@capacitor/app`の`backButton`リスナーを一度も登録していなかったため、Androidのデフォルト挙動（戻る操作＝アプリ終了）がそのまま素通りしていた。

**実装内容**:
- `@capacitor/app`を新規インストール（`^8.1.1`、他のCapacitorプラグインと同じ8系）
- `App.jsx`に`backButton`リスナーを追加。押された時に、開いているものを優先順位順に1つずつ閉じる（各種モーダル → スレッド表示 → 開いているチャット→チャット一覧に戻る）。何も開いていない状態（＝アプリのルート画面）まで戻ったら、そこで初めて`CapacitorApp.exitApp()`で終了——これは他のAndroidアプリと同じ標準的な挙動
- `npx cap sync android`を実行し、ネイティブAndroidプロジェクト（`capacitor.build.gradle`・`capacitor.settings.gradle`）にプラグインを反映済み

**⚠️ 重要: これは今までの「Web版デプロイだけで即反映」パターンが使えない**（`748b8dd`のCapacitorライブ更新化の対象外）。理由: ライブ更新の仕組みは`capacitor.config.ts`の`server.url`が本番Workerを向いていることでJS/CSSの変更を即時反映するものだが、**今回はネイティブプラグイン自体を新規追加**しているため、既存インストール済みのアプリにその機能（バックボタンを橋渡しするネイティブコード）が存在しない。**Codemagicでの新規ビルド＆配信、既存インストール分の更新（またはPlay Store経由の自動更新）が別途必要**。JS側の変更は本番Workerに反映済みだが、これだけでは実機の挙動は変わらない。

**次にこの作業をする人へ**: 次にCodemagicでビルドするタイミングで、この変更が確実に含まれていることを確認すること（`frontend/package.json`に`@capacitor/app`が入っているか、`android/`配下の変更がコミットに含まれているかを見ればわかる）。ビルド後は実機で「モーダルを開いて戻る」「チャットを開いて戻る」「何もない状態で戻る＝アプリ終了」の3パターンを確認すると安心。

---

## ✅ 2026-08-21：FCM通知の原因調査 + 受電分析ダッシュボード実装（本番反映済み）

**① FCM通知の原因調査**:
- `frontend/android/app/src/main/AndroidManifest.xml`に**`android.permission.POST_NOTIFICATIONS`の宣言が無かった**ことを発見。`targetSdkVersion = 36`（Android 15+相当）のため、Android 13以降はこの宣言が無いと権限リクエスト自体が（ポップアップも出さずに）拒否される仕様——「許可ポップアップも出ず」という報告と一致する典型的な不具合。`<uses-permission android:name="android.permission.INTERNET" />`の下に追加・修正済み（コード上は修正済みだが**Codemagicでの新規ビルド配信が必要**、上記の戻るボタン修正と同じく未実施）
- `google-services.json`のパッケージ名（`com.easystance.connectsuite`）は`applicationId`と一致していることを確認、こちらは問題なし
- **⚠️訂正: `fcm_tokens`テーブルを実際に確認したところ、「登録が一度も成功していない」という前回セッションの記録は古い情報だった。** 8/20 13:27以降、小林伸彰（オーナー本人）・小林衣里（奥様）の端末で計4回、登録が実際に成功している（直近は8/21 12:47）。おそらく前回セッションの案内通り、端末の「設定→アプリ→通知」を手動でオンにしたことで解消したと推測される。**残る未確認事項は「登録は成功しているが、実際に通知が画面に表示される（鳴る）かどうか」**——これは今回のマニフェスト修正が効いてくる可能性がある領域（トークン登録とポップアップ表示は別物で、宣言漏れがあると登録自体は通っても表示側だけブロックされるケースがある）。次にCodemagicでビルドを配信したあと、オーナー本人に実際に通知が鳴るか確認してもらうこと

**② 受電分析ダッシュボード実装**:
- `GET /api/call-memos/analytics`（`backend/worker.js`、`STAFF_ROLES`=owner/adminのみ、セッション必須）を新設。返す内容: 概況（総件数・未対応・対応中・完了・平均対応時間）、取引先別件数、宛先別件数（DM/グループ/部門）、対応者別件数・平均対応時間、曜日別・時間帯別の傾向、直近30日の日別推移
- 時間帯・曜日集計は`created_at`（タイムゾーン情報なしのUTC保存）を`datetime(created_at, '+9 hours')`でJST補正してから集計——`frontend/src/utils/datetime.js`の表示側補正と同じ考え方をSQL側でも踏襲
- フロント: `frontend/src/components/CallAnalyticsView.jsx`（新規）。CSSのみのシンプルな棒グラフ・時間帯チャート（外部チャートライブラリは追加していない）。`CallSyncApp.jsx`のサブナビに「📊 受電分析」タブを追加、owner/adminにのみ表示
- 検証: ビルド成功・本番デプロイ済み。実際のオーナー権限アカウントでのブラウザ確認は未実施（認証情報を持っていないため）——**次にログインした際に「電話メモ→受電分析」タブの表示を確認してほしい**（小林家org=2には4件のテストデータがあるので、空にはならないはず）

**次にこの作業をする人へ**: CTI連携（クラウドPBXとの着信ポップアップ連携）はまだ未着手のまま。分析ダッシュボードのデータが十分溜まってきたら、対応者別の負荷偏り（平均対応時間の差）などをオーナーに見てもらうと良さそう。

---

## ✅ 2026-08-21（続報）：部門ごとの受電カテゴリ機能を追加（本番反映済み）

**背景**: オーナーから「電話の種別で分析したい。例えば寝具リース課なら貸布団・クリーニング・固定客注文・その他、みたいな部門ごとに違う分類を作れないか」という相談。既存の`call_type`（折り返し希望／緊急／伝言のみ＝緊急度の分類）とは別軸の、業務内容の分類として新設。入力は**任意**（オーナー確定）——部門にカテゴリを設定していない場合は今まで通りスキップでき、既存の運用に影響しない。

**実装内容**:
- `migrations/0011_call_categories.sql`: `call_categories`テーブル新設（`department_id`・`organization_id`・`label`・`sort_order`）、`call_memos`に`category_id`列を追加。**カテゴリ削除時は既存メモの`category_id`をNULLにする（`ON DELETE SET NULL`）、部門削除時はその部門のカテゴリも連動削除（`ON DELETE CASCADE`）**——ソフトデリートの回でも触れた「FK制約で削除できなくなる」不具合を、今回は先回りしてON DELETE句を最初から付けることで回避した
- `GET/POST/PUT/DELETE /api/call-categories`（作成・更新・削除は管理者権限必須、`worker.js`）
- 管理者メニュー「部門マスタ管理」タブに、各部門の行に「📋 受電カテゴリ」ボタンを追加。クリックで展開し、その部門専用のカテゴリを追加・編集・削除できる（`AdminModal.jsx`の`CallCategoryManager`）
- 受電メモ登録モーダル（`NewCallMemoModal.jsx`）: 宛先から部門を解決できた場合（部門宛て、またはDM宛てでその人の所属部門が分かる場合）のみ、その部門のカテゴリがチップ選択式で表示される。グループ宛ての場合は部門が一意に決まらないため非表示（今回はスコープ外）
- 受電分析ダッシュボードに「受電カテゴリ別」の内訳を追加（`部門名 - カテゴリ名`の形式で表示）

**次にこの作業をする人へ**: 今回はチャット上の受電メモカード（`CallMemoCard.jsx`）や電話メモ専用通知欄（`CallMemoTray.jsx`）にはカテゴリを表示していない（分析での集計は既に効く状態だが、登録後にカテゴリを見返す手段が分析画面以外に無い）。オーナーから要望が出たら、`CallMemoCard.jsx`にカテゴリのバッジを1つ追加するだけで対応できるはず（データは`GET /api/call-memos`のレスポンスに`category_label`として既に含まれている）。

---

## ✅ 2026-08-21（実機テストで発覚した3件を修正・本番反映済み）

オーナーが実際に「デスクモニター」画面から受電メモを登録して分かった問題:

1. **カテゴリの入力欄が誤解を招く書き方だった**: 管理画面の入力例が「貸布団 / クリーニング / 固定客の注文」とスラッシュ区切りだったため、1つのカテゴリとして丸ごと入力されてしまった（実際に本番で「貸布団（個人）/クリーニング（個人）」という1件のカテゴリが作られていた）。プレースホルダーを単一の例に変更し、「1つずつ入力して追加してください」という注意書きを追加。**すでに本番に入っていた誤入力データはこちらでDBを直接修正し、2件に分割済み**
2. **会社名が必須でつらい**: 「相手先は個人のお客様も多い」との指摘で、`NewCallMemoModal.jsx`・`DeskMonitorView.jsx`両方の会社名入力を任意化（会社名かお客様名のどちらか一方があればOK）。表示側（`CallMemoCard.jsx`）も、会社名が空なら👤アイコン＋お客様名だけを表示するよう調整
3. **カテゴリを選んでも保存されるだけで、どこにも表示されない**: 原因は`GET /api/messages`（チャットに流れる受電カードの取得クエリ）が`call_categories`をJOINしていなかったこと。`GET /api/call-memos/analytics`には別途JOINがあったため分析画面では件数が反映されていたが、肝心のチャットカードには表示されないという分かりにくい「半分だけ動いている」状態になっていた。JOIN追加＋`CallMemoCard.jsx`・`CallMemoTray.jsx`にカテゴリバッジを追加して解消。**分析グラフのラベルも「部門名 - カテゴリ名」が幅不足で「寝具リース課 …」のように省略されて読めなくなっていたため、折り返し表示に変更**

**次にこの作業をする人へ**: `call_memos`に新しい列を追加する時は、少なくとも3箇所（`GET /api/call-memos`・`GET /api/messages`のdm/group分岐・`GET /api/call-memos/analytics`）それぞれに個別のJOINが必要になる。`grep -n "LEFT JOIN call_memos cm"`で該当箇所を洗い出してから着手すると同じ抜け漏れを防げる。

---

## ✅ 2026-08-21：スマホの現場ビューが組織全員分の受電メモを表示していた不具合を修正（本番反映済み）

**背景**: オーナーから「スマホの現場ビューは、ログインしている本人が対応すべきもの（所属部門も含めて）だけが未対応リストに載ってほしい」という指摘。

**原因**: `MobileViewMode.jsx`の「📱 現場・マイ受電一覧」は、名前に反して**ステータス（未対応/完了）でしか絞り込んでおらず、宛先による絞り込みが一切無かった**——組織の全メンバーが自分のスマホで組織全体の受電メモを見ている状態だった。PC版の「デスクモニター」（事務員が全体を俯瞰する用途）と「ボード」画面には既に「自分宛て」フィルターがあったが、スマホの現場ビューだけ抜けていた。

**修正内容**: `MobileViewMode.jsx`に、①自分宛て（DM）②自分の所属部門宛て（`currentUser.department_id`と一致）③自分が入っているグループ宛て、のいずれかだけを表示するフィルターを追加。グループの判定は`CallSyncApp.jsx`の既存の「自分のグループ」判定ロジック（`member_ids`に自分のIDが含まれる、または自分が作成者）と同じ考え方を踏襲。`groups`をpropとして新たに受け取るよう変更（`CallSyncApp.jsx`から渡すように修正）。

**次にこの作業をする人へ**: 「自分宛てだけを見せる」ロジックが今、`CallSyncApp.jsx`（ボード画面のmyCalls）・`CallMemoTray.jsx`・`MobileViewMode.jsx`の3箇所に同じような形でバラバラに実装されている。将来また同種の不具合（一部の画面だけ絞り込み漏れ）が起きやすい状態なので、共通のヘルパー関数（例: `isCallMemoForUser(memo, user, groups)`）に切り出すことを検討する価値がある。
