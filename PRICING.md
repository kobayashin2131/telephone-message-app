# Connect Suite 料金体系（決定事項）

最終更新: 2026-08-29（オーナー確定・実装はまだ）

## 基本方針
- **課金単位は「組織単位の人数帯固定料金」**（1ユーザーごとの従量課金ではない）
- 参考: HOMEBASE（同オーナーの別プロダクト）と同じ考え方・同じ価格帯を採用し、自社プロダクトラインとしての一貫性を持たせる

## プラン

| プラン | 対象人数目安 | 月額（税別） |
|---|---|---|
| スモール | 〜15名 | ¥3,000 |
| スタンダード | 〜40名 | ¥6,000 |
| ビジネス | 〜80名 | ¥10,000 |
| エンタープライズ | 81名以上 | 個別見積もり |

- 機能自体はどのプランも同じ（受電分析ダッシュボードなどを上位プラン限定にするかは今回は決めていない、将来の検討事項）

## 標準ストレージ容量（プラン込み）

| プラン | 標準容量 |
|---|---|
| スモール | 3GB |
| スタンダード | 8GB |
| ビジネス | 18GB |
| エンタープライズ | 応相談 |

`organizations.storage_limit_bytes`列は実装済み、プラットフォーム管理者側で組織ごとに個別設定可能。

## 追加ストレージパック（オプション）

| パック | 追加容量 | 月額（税別） |
|---|---|---|
| 追加パックA | +10GB | ¥500 |
| 追加パックB | +30GB | ¥1,500 |

短い動画添付（後述）に対応したことで、写真・PDFのみを想定していた時より容量消費が増える見込みのため、標準容量・追加パックとも当初案より引き上げ済み。

## 無料トライアル

**「登録した月の翌月末まで無料」**

- 例: 8月15日に登録 → 9月30日まで無料（約1.5ヶ月）
- 例: 8月1日に登録 → 9月30日まで無料（約2ヶ月）
- 例: 8月31日に登録 → 9月30日まで無料（約1ヶ月）

登録日から固定日数（例: 30日）でカウントする方式は、月末登録者が不利になり不公平なため不採用。「登録月＋翌月まるごと」にすることで、最短でも1ヶ月強は必ず試せるようにしつつ、カレンダー月の区切りに揃えることで請求サイクル（会費ペイ）との連携もシンプルにする。

## トライアル終了後（未契約の場合）

**閲覧のみ可能にする**（強制ログアウトやデータ削除はしない）。

- できる: 過去のチャット履歴・電話メモの閲覧
- できない: 新規チャット投稿、新規電話メモ登録、新規ユーザー追加など、すべての新規作成・更新操作
- 契約後は即座に通常利用に戻る（データはそのまま保持されているため復旧作業は不要）

この設計は、組織の「解約」時（ログイン停止だがデータは保持、再開すれば復帰）と同じ考え方を踏襲している。

**督促・ロックのタイミング（HOMEBASEの既存実装と同じ考え方を将来Connect Suiteにも適用する）**:
- 登録**1ヶ月目**＝登録した月そのもの（無料）
- 登録**2ヶ月目の25日**から、会費Payへの支払い方法登録を促す督促を開始（3日おきに再送）
- 登録**3ヶ月目の12日**が会費Pay側の実際の登録締切（会費Payは毎月12日までの登録分だけが当月26日の請求に間に合う仕様、変更不可・ヘルプセンターで確認済み）
- **13日**時点で未登録なら閲覧専用モードにロック（登録が完了すればすぐに解除）

## 動画添付（2026-08-29追加・実装済み）

チャットに短い動画（mp4 / iOSの.mov / webm、最大30MB、長時間の動画は非対応）を添付できる機能を追加した。写真・PDFの上限（15MB）とは別に、動画だけ30MBまでの枠を用意している。上記のストレージ容量見直しはこの機能追加が理由。

## 実装状況（2026-08-29更新）

- ✅ **トライアル終了日の計算・表示**: `getFreeTrialEndDate()`をHOMEBASE（`D:\baseball-saas\src\lib\billing.ts`）から移植し、`GET /api/organization`のレスポンスに`trial_end_date`として追加。組織オーナーの「プラン・容量」タブに「◯年◯月◯日以降、正式なプランのご案内を予定しています」と表示するところまでは完了・本番反映済み
- ✅ **動画添付**: 上記の通り実装・本番反映済み
- ⬜ **機能制限（閲覧専用モード）は未実装**: 決済登録の導線が無い状態でロックすると復旧手段が無くなるため、意図的に後回しにしている
- ⬜ **会費Pay連携そのものが未着手**: HOMEBASE側は`src/lib/kaihipay.ts`（APIクライアント）・`src/app/api/cron/billing-check`（督促・ロックの定期実行）まで実装済みで、参考にできる実装が存在する。Connect Suite側で今後必要になるのは:
  1. 会費Pay管理画面でConnect Suite用のコース（プラン別金額）を手動作成
  2. `organizations`テーブルに支払い状態列を追加（`kaihipay_customer_number`・`payment_method_registered_at`・`trial_locked_at`など、HOMEBASEの`Team`モデルと同じ考え方）
  3. 支払い方法登録のUI画面（氏名・カナ・住所・電話番号・メールが必要、会費Pay側の会員登録項目に合わせる）
  4. 督促・ロックの定期実行（HOMEBASEの`billing-check`をベースに、Connect Suite用の人数課金・成人数上限超過ロジックに置き換える）
- 決めていないこと: 上位プランだけの機能差別化の有無、決済失敗時の猶予期間、年払い割引の有無
